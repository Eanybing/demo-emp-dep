package com.ec.empdep.config;

/**
 * Utilty enum to define application profiles. Simple to avoid typos!!!
 * 
 * @author ecartaxo
 *
 */
public enum Profiles {
	DEVELOPMENT, PRODUCTION, UNIT_TESTS, INTEGRATION_TESTS
}
